include:
  - db_yml/${DB_ENGINE}.yml
  - worker.yml
services:
  nginx:
    build: ./nginx
    ports:
      - ${DEPLOY_PORT}:${DEPLOY_PORT}
    volumes:
      - static_volume:/montrek/static
      # MEDIA_ROOT is the repo-relative montrek/uploads, shared by the host and
      # every container through the .:/montrek bind mount; nginx has no repo
      # mount of its own, so it binds the same directory directly.
      - ./montrek/uploads:/montrek/uploads:ro
      # Defaults to the repo's own certs; point TLS_CERT_DIR at /etc/letsencrypt
      # on internet-facing hosts to serve an auto-renewed certificate.
      - ${TLS_CERT_DIR:-./nginx/certs}:/etc/tls:ro
    depends_on:
      - web
    environment:
      - APP_PORT=${APP_PORT}
      - DEPLOY_PORT=${DEPLOY_PORT}
      - DEPLOY_HOST=${DEPLOY_HOST}
      - PROJECT_NAME=${PROJECT_NAME}
      - ENABLE_KEYCLOAK=${ENABLE_KEYCLOAK}
      - KEYCLOAK_PORT=${KEYCLOAK_PORT}
      - FLOWER_PORT=${FLOWER_PORT}
      - SSL_CERT_PATH=${SSL_CERT_PATH:-/etc/tls/fullchain.crt}
      - SSL_KEY_PATH=${SSL_KEY_PATH:-/etc/tls/cert.key}
      - DNS_RESOLVER=${DNS_RESOLVER:-127.0.0.11}
    restart: "unless-stopped"
    networks:
      - montrek_network

  web:
    image: ${CONTAINER_REGISTRY:-ghcr.io}/montrek-software/montrek-container:latest
    user: "0:0"
    environment:
      - REQUESTS_CA_BUNDLE=/etc/ssl/certs/ca-certificates.crt
      - SERVICE=web
      - PIUD=${USER_ID}
      - PGUD=${GROUP_ID}
    volumes:
      - .:/montrek
      - static_volume:/montrek/static
    env_file:
      - .env
    expose:
      - ${APP_PORT}
    ports:
      - ${APP_PORT}:${APP_PORT}
    depends_on:
      - db
      - redis
    restart: "unless-stopped"
    networks:
      - montrek_network

  celery_beat:
    image: ${CONTAINER_REGISTRY:-ghcr.io}/montrek-software/montrek-container:latest
    user: "0:0"
    environment:
      - SERVICE=celery_beat
      - PIUD=${USER_ID}
      - PGUD=${GROUP_ID}
    volumes:
      - .:/montrek
    env_file:
      - .env
    depends_on:
      - web
      - db
      - redis
      - parallel_worker
      - sequential_worker
      - fast_worker
    restart: "unless-stopped"
    networks:
      - montrek_network

  redis:
    image: redis:7-alpine
    restart: "unless-stopped"
    networks:
      - montrek_network

  flower:
    image: mher/flower:2.0.1
    command: celery flower -A montrek.celery_app --port=${FLOWER_PORT} --broker={CELERY_BROKER_URL} --basic_auth=admin:${ADMIN_PASSWORD}
    ports:
      - ${FLOWER_PORT}:${FLOWER_PORT}
    env_file:
      - .env
    depends_on:
      - web
      - redis
      - sequential_worker
      - parallel_worker
      - fast_worker
    restart: "unless-stopped"
    networks:
      - montrek_network

  keycloak-db:
    image: postgres:16
    environment:
      POSTGRES_DB: keycloak
      POSTGRES_USER: keycloak
      POSTGRES_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
    volumes:
      - keycloak_db_data:/var/lib/postgresql/data
    restart: "unless-stopped"
    networks:
      - montrek_network
    profiles:
      - keycloak
  keycloak:
    image: quay.io/keycloak/keycloak:24.0
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: ${ADMIN_PASSWORD}
      KC_DB: postgres
      KC_DB_URL: jdbc:postgresql://keycloak-db:5432/keycloak
      KC_DB_USERNAME: keycloak
      KC_DB_PASSWORD: ${KEYCLOAK_DB_PASSWORD}
      KC_PROXY: edge
      KC_HTTP_ENABLED: "true"
      KC_HOSTNAME: auth-${PROJECT_NAME}.${DEPLOY_HOST}
      KC_HOSTNAME_STRICT: "false"
      KEYCLOAK_REALM: ${KEYCLOAK_REALM}
      KEYCLOAK_CLIENT_ID: ${KEYCLOAK_CLIENT_ID}
      KEYCLOAK_CLIENT_SECRET: ${KEYCLOAK_CLIENT_SECRET}
      PROJECT_NAME: ${PROJECT_NAME}
      DEPLOY_HOST: ${DEPLOY_HOST}
      ADMIN_EMAIL: ${ADMIN_EMAIL}
      PRIMARY_COLOR: ${PRIMARY_COLOR}
      SECONDARY_COLOR: ${SECONDARY_COLOR}
    volumes:
      - ./keycloak/bin/init-realm.sh:/usr/local/bin/init-realm.sh:ro
      - ./keycloak/themes/montrek:/opt/keycloak/themes/montrek
    entrypoint: ["/bin/sh", "/usr/local/bin/init-realm.sh"]
    depends_on:
      - keycloak-db
    restart: "unless-stopped"
    networks:
      - montrek_network
    profiles:
      - keycloak

volumes:
  db_data:
  static_volume:
  keycloak_db_data:

networks:
  montrek_network:
    driver: bridge
