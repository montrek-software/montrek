services:
  db:
    image: alpine:3.19 # super lightweight
    command:
      [
        "sh",
        "-c",
        "echo 'Dummy DB service — using External DB instead'; sleep infinity",
      ]
    restart: "unless-stopped"
    networks:
      - montrek_network
